---
description: 企业知识中心核心定位——人读与 AI 共用知识，不是传统 Wiki
alwaysApply: true
---

# 产品定位

本项目是企业知识中心，不是传统 Wiki。

- 同一份知识同时服务员工（Web 阅读/编辑/检索）和智能体 / AI 工具（Agent API、MCP）。
- 先治理知识（空间、权限、版本、发布、审计），再向人和 AI 开放。
- 不要做成只给人看的文档站，也不要做成以问答/RAG 为中心的产品。
- 关闭 LLM 后，知识的创建、浏览、搜索、权限、版本仍必须可用。
- 给 AI 的接口与 Web 共用数据与权限，必须鉴权、过滤可见性、记审计。

新增能力时同时问：员工能不能用？智能体能否在权限范围内取到同一份知识？
