# 开发约束

## 产品定位（必须遵守）

本项目是**企业知识中心**，不是传统 Wiki。

- 同一份知识同时服务两类使用者：员工（在线阅读、编辑、检索）和智能体 / AI 工具（API、MCP）。
- 先把知识作为企业资产管清楚（空间、权限、版本、发布、审计），再向人和 AI 开放。
- 不要做成「只给人看的文档站」，也不要做成「以问答/RAG 为中心的产品」。AI 是知识的消费者，不是知识本身。
- 关闭 LLM / Embedding / RAG 后，创建、编辑、浏览、搜索、权限、版本仍必须可用。
- Agent 入口与 Web 入口共用同一套数据与权限；给 AI 的接口必须鉴权、按可见性过滤、记审计。

做功能时先问：员工能不能用？智能体能不能在权限范围内取到同一份知识？缺任一侧，就还不是知识中心。

## 工程约束

- 采用 Monorepo 目录结构。
- 前端：Vue 3 + TS + Vite + Tailwind + Element Plus。
- 后端：FastAPI + SQLAlchemy + Alembic。
- 数据库：MySQL 8（默认）。
- 业务逻辑分层清晰（API -> Service -> Model）。
- 所有功能需通过自动化验证。
