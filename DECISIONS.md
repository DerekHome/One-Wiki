# 关键架构决策

## 产品定位

- 本项目定位为企业知识中心，不是传统 Wiki。
- 知识是企业资产：Web 供员工阅读与协作，Agent API / MCP 供智能体与 AI 工具取用。
- 人与 AI 共用同一套知识、权限和版本；AI 是消费者，不能替代知识治理。
- Agent 使用独立 API Key（`kck_`），只读 `/api/v1/agent`。每个 Agent 可配置说明、可见空间与只读能力；MCP 使用同一套身份，禁止管理员回落。
- 草稿/归档只给人看；智能体只能读取已发布最新版本，并必须带引用 URI。
- 核心能力（空间、权限、版本、检索、审计）不依赖 LLM；问答 / RAG 属于可叠加模块。

## MVP 技术栈
- 采用单体架构，便于研发维护。
- 使用Pydantic进行数据验证，保证API安全性。
